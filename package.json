{
  "name": "sistema-extrato-dominio",
  "private": true,
  "version": "0.1.0",
  "description": "Extrato bancário -> Lançamentos contábeis em lote (Leiaute Domínio Sistemas)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend,parser -c cyan,magenta,yellow \"npm:dev:frontend\" \"npm:dev:backend\" \"npm:dev:parser\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:parser": "node scripts/parser.mjs dev",
    "build": "npm run build -w frontend && npm run build -w backend",
    "test": "npm run test -w backend && npm run test:parser",
    "test:parser": "node scripts/parser.mjs test",
    "lint": "npm run lint -w frontend && npm run lint -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
